# OCR 方案评估

更新日期：2026-08-08

## 目标

资料库 OCR 必须满足以下约束：

- Windows 与 Python 3.12 可直接运行，默认使用本机 CPU。
- 中文、英文、数字和常见科学符号可用。
- 返回逐区域文字、置信度和坐标，而不只是生成一个搜索版 PDF。
- 原 PDF 保持只读；所有 OCR 结果都是带哈希和版本的可重建派生数据。
- 扫描页、原生文本页和混合 PDF 使用同一套页码与引用契约。
- 后续可以接入版面、表格、公式和视觉模型，但首个版本不强制安装大型 VLM。

## 候选比较

| 项目 | 本地 Windows | 中文与坐标 | 版面/表格/公式 | 许可证 | 本项目判断 |
|---|---|---|---|---|---|
| [RapidOCR](https://github.com/RapidAI/RapidOCR) 3.9.2 | 是，ONNX Runtime CPU | 默认中英文，返回文字框和置信度 | 基础 OCR，不负责完整文档结构 | Apache-2.0 | 首个适配器；依赖较轻、启动和集成简单 |
| [PaddleOCR](https://github.com/PaddlePaddle/PaddleOCR) 3.7.0 | 是 | 强，PP-OCRv6 支持中文及多语言 | PP-StructureV3 提供布局、表格和细粒度坐标 | Apache-2.0 | 第二阶段结构化解析候选，默认依赖偏重 |
| [Docling](https://github.com/docling-project/docling) 2.118.1 | 是 | 可使用 RapidOCR 等后端 | 支持阅读顺序、表格、公式、图片和多种格式 | MIT（模型另看各自许可证） | 第二阶段多格式和版面适配器候选 |
| [OCRmyPDF](https://github.com/ocrmypdf/OCRmyPDF) 17.10.0 | 支持，但依赖 Tesseract 等外部组件 | 多语言文本层 | 重点是 PDF/A、纠偏和搜索层，不是内部区域证据模型 | MPL-2.0 | 适合作为“导出可搜索副本”，不作为主解析器 |
| [MinerU](https://github.com/opendatalab/MinerU) | 可部署，官方列出 Windows 优化 | 强文档解析能力 | 阅读顺序、图、表、公式和结构化 Markdown/JSON | MinerU Open Source License（基于 Apache-2.0 的定制许可证） | 能力强，但模型资源、定制许可证和部署复杂度高于首版方案 |

## 选择

首版采用 `RapidOCR + ONNX Runtime + PyMuPDF`：

1. PyMuPDF 检查每页原生文本层；内容足够时不运行 OCR。
2. 缺少可用文本的页面按 120 DPI 渲染为 PNG，在本机交给 RapidOCR。
3. OCR 行按阅读顺序合并为检索片段，保存平均置信度和归一化坐标。
4. 页面 PNG 与 JSON 按原文件 SHA-256、OCR 引擎版本和解析版本缓存。
5. 检索结果通过受限 API 返回原页图像，前端按坐标覆盖证据框。

RapidOCR 不负责准确还原复杂公式和表格结构。OCR 文本用于召回，原页用于证据核验；公式、下标、希腊字母和表格数值不能只依赖 OCR 输出。

## 真实样书基线

测试文件：`《物理化学 上 第6版》`，48,408,429 字节，324 页。

- 抽样结果显示前 323 页为单页图像且没有文本层，第 324 页有少量文本。
- 120 DPI 单页 RapidOCR 初测识别 38 个区域，约 6 秒。
- 项目完整单页管道生成 5 个带坐标检索片段，首片段平均置信度约 96.4%。
- 整书首次处理耗时 1766.5 秒（约 29 分 27 秒）：323 个 OCR 页、1 个原生文本页、0 个不可读页，生成 2227 个检索片段。
- 323 页共识别 16,615 个文字区域，区域平均置信度约 95.43%；派生 PNG、JSON 和清单共 98,861,796 字节（94.28 MiB）。
- 同文件第二次导入完整复用 323 页缓存，耗时约 24.98 秒，约为首次处理的 1/70；文档未重建，原 PDF SHA-256 前后一致。
- 关键词检索可在第 82 页命中“标准摩尔生成焓”，在第 168、210、269 等页命中“氯仿”，并返回置信度和证据坐标。
- 本地 `BAAI/bge-small-zh-v1.5` + Qdrant 在约 151 秒内为 2227 个片段建好向量；“水和氯仿的正常沸点”语义检索首位为第 168 页，“半透膜两侧为什么产生渗透压”前三位均为第 210 页。
- 桌面与 390 px 移动视口均可打开原页；第 168 页的 3 个证据框准确覆盖题目和相邻答案行，浏览器控制台无错误。
- 正文召回可用；公式下标仍有误识别，而且高置信度不等于公式或表格结构正确，因此必须保留原页高亮与后续视觉复核入口。

## 后续阶段

1. 用 Docling 或 PP-StructureV3 增加章节、双栏、图、表和公式区域类型。
2. 对低置信度区域，以及公式、表格、图题等高风险区域提供显式的视觉模型复核；不能只按 OCR 置信度决定是否复核，也不默认外发整页或整本书。
3. 将人工修订保存为覆盖层，保留 OCR 原结果、修订人和时间。
4. PaperQA2 建索引时优先复用资料库的 OCR 派生文本，避免两个索引看到不同内容。
5. 增加取消、队列优先级和资源限制；当前页级缓存已经支持失败后续跑。
